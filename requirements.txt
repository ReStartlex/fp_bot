# --- Core ---
httpx[socks]>=0.27.0           # async HTTP-клиент для ns.gifts + поддержка SOCKS5 для Telegram
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
aiosqlite>=0.20.0
APScheduler>=3.10.0
loguru>=0.7.2
pyotp>=2.9.0                   # TOTP для 2FA ns.gifts (если включено)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- FunPay ---
# FunPayAPI используем только для long-poll событий чата (golden_key хватает).
# Админ-операции с лотами идут через свой HTTP-клиент (admin_http.py),
# потому что в установленной FunPayAPI get_lot_fields/save_lot ожидают
# JSON, а FunPay отдаёт HTML — парсим HTML сами.
#
# ВАЖНО: НЕ закрепляем версию requests/beautifulsoup4 здесь — FunPayAPI
# тащит свой requests (обычно <2.31), и жёсткое `requests>=2.31` ломает
# pip resolver (ResolutionImpossible). Те версии, что приходят с
# FunPayAPI, для нашего admin_http полностью достаточны.
FunPayAPI
beautifulsoup4

# --- Telegram ---
aiogram>=3.5.0

# --- Авто-курс USD/RUB (auto-режим) ---
# cbr-xml-daily.ru отдаёт JSON, лишних зависимостей не нужно

# --- Dev ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
