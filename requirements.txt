# --- Core ---
httpx[socks]>=0.27.0           # async HTTP-клиент для ns.gifts + поддержка SOCKS5 для Telegram
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
aiosqlite>=0.20.0
APScheduler>=3.10.0
loguru>=0.7.2
pyotp>=2.9.0                   # TOTP для 2FA ns.gifts (если включено)

# --- FunPay (неофициальная библиотека) ---
FunPayAPI>=0.1.34

# --- Telegram ---
aiogram>=3.5.0

# --- Авто-курс USD/RUB (auto-режим) ---
# cbr-xml-daily.ru отдаёт JSON, лишних зависимостей не нужно

# --- Dev ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
